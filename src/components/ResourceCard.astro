---
import { formatDate } from 'src/utils/dates'

interface Props {
  title: string
  description: string
  category: string
  href: string
  date: Date
}

const { title, description, category, href, date } = Astro.props
---

<article
  class=`border border-gray-700 rounded-lg p-6 hover:shadow-lg transition-shadow bg-gray-800 height-[auto]`
>
  <div class='mb-2'>
    <h3 class='text-xl font-bold mb-2'>
      <a href={href} class='text-[#368beb] hover:text-[#83d6e7]'>
        {title}
      </a>
    </h3>
    <p class='text-gray-300 text-xs mb-2'>{description}</p>
    <p class='text-sm text-gray-400'>
      {formatDate(date.toISOString())}
    </p>
  </div>
  <div class='flex items-center gap-2 text-sm'>
    <span class='bg-[#314aa9] text-violet-200 px-2 py-1 rounded'>
      {category}
    </span>
  </div>
</article>
